## What

<!-- One or two sentences: what does this PR change? -->

## Why

<!-- The problem or use case. Link the issue if there is one: Fixes #123 -->

## How it was verified

- [ ] `pnpm compile` passes
- [ ] `pnpm test` passes (add/adjust tests when touching `utils/`)
- [ ] `pnpm build` produces a clean bundle
- [ ] Tested in DevTools (extension reloaded + DevTools reopened)
- [ ] If output format changed: example TOON export attached below

<!-- Paste a short TOON excerpt or screenshot when the UI/export changes -->

## Notes

- [ ] No capture data leaves the machine; redaction stays always-on
- [ ] The panel remains free of extension APIs (landing demo reuses it)
